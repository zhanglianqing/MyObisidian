---
description: 用户发送小红书分享链接时，抓取并归类到 Clippings
globs:
  - Clippings/**/*
alwaysApply: false
---

# 小红书剪藏（Cursor）

用户消息含 `xiaohongshu.com` 或小红书「分享复制」全文时：

## 1. 抓取（选模式）

**默认 `social`**（育儿 / 观点 / 一般社交）——转写但不存 mp4：

```powershell
cd "e:\Obisidian\MyObisidian\0 工作流\scripts"
python clip_xhs_auto.py "<分享全文>" --mode social
```

**`radiology`**（阅片、影像、MRI、CT、核磁、放射、超声、教学视频）——本地 mp4 + 转写：

```powershell
python clip_xhs_auto.py "<分享全文>" --mode radiology
```

从输出 JSON 读取 `file` 路径。

## 2. 归类（必须编辑 md，勿只回复摘要）

### 2.1 元数据

- `ai_summary` ≤80 字，写**可检索的结论**（非「讲了某话题」）
- `tags` ≤3；`status: classified`；`action`
- **project**：
  - `radiology`：**必须**含 `[[临床医学知识库]]`（脚本已预填，可再加 1 个）
  - `social`：从下列选 0～2 个  
    `[[00 工作系统3.0：海马+抑郁症+认知损伤的机制及诊疗]]`、`[[临床医学知识库]]`、`[[5 分支项目/5.1 眉彡/00_ResearchTrack]]`、`7 可复用知识库`、`[[日常随想]]`

### 2.2 要点（所有类型都要「厚」）

| 类型 | ## 要点 |
|------|---------|
| **视频** | **8～12 条**，每条一句完整话：是什么 / 为什么 / 怎么做 / 具体名称 |
| **图文** | **6～8 条**，同样写完整句；把正文里的步骤、工具、结论拆清楚 |

禁止只写 3 条笼统话（如「讲了 Git」「值得学习」）。

### 2.3 内容纪要（凡有转写必填）

笔记里只要有 **`转写·原文`**（`<details>` 内 Whisper 稿），**必须**填写 **`## 内容纪要`**：

- 根据口述稿**重写**，去语气词（嗯、那个、就是、好吧）
- **分段或小标题**；保留书名、分级、征象、操作步骤等**具体信息**
- **勿照抄** `<details>` 内的口述稿
- 篇幅通常为要点总字数的 **2～4 倍**

**图文帖**无转写区块时，**不写**内容纪要；把信息写进厚要点即可。

### 2.4 结构保留

- **转写·原文**：`<details>` 内 **保留不动**，仅折叠备查
- **影像视频（本地存档）**：radiology 模式保留 `![[...mp4]]`
- **正文**、封面图：保留

### 2.5 禁止

- 不要添加 `video_url`、`<video>`、外链 mp4（social 模式）

## 3. 回复

说明路径、模式；提醒日常阅读 **要点 + 内容纪要**（有转写时），口述稿在折叠区。
