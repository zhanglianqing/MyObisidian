---
description: 本周工作台 §一 排班与空档规则（ICS 同步、不可用时段、可用时段）
globs: 0 工作流/本周工作台.md,0 工作流/workflows/3.4 Workflow*.md,0 工作流/scripts/weekly_workbench_from_ics.py
alwaysApply: false
---

# 每周工作台 · 不可用时段与空档

## 表格

- **周表**：**周一～周日** 共 7 行；`week_of` = 该周**周一**日期。`--week` 可传周内任意一天，脚本归到周一。
- **日期列**：真实星期 + 日期（如 `周六 05-23`）。
- **不可用时段**（原「班次」列，2026-05-24 定稿）：写**占用的时段**——ICS 班表简写 + 手填固定事项（会议、幼儿园、飞机等）；多段用 ` + ` 或 `;`。
- **ICS 班表简写**：**7T**（原 MR13）/ **早班** / **下午班** / **晚班** / **夜班** / **补休** / **休**。
- **带娃 / 火山**：默认 **20–21 带读**；当日有 **晚班** → `—`（与 20–21 冲突）。**夜班**若 22:30 后上岗，20–21 仍可带读。

## 预计可用（只写空档）

- **不写「xx不可用」**——不可用列已体现；本列只写能用的时段。
- **仅下午班**：上午 + 晚上可规划（科研）。
- **仅夜班**：白天 + 晚上可规划（科研）。
- **7T + 晚班**：09:00–17:00 7T 可科研。
- **补休**：15:00–20:00 可轻量。
- **休班**：全天可规划（科研）。

## 脚本

`python "0 工作流/scripts/weekly_workbench_from_ics.py" --week <week_of> --markdown`  
输出对应 **不可用时段**（ICS 部分）与 **预计可用**、**带娃** 三列；固定事项（会议/出行等）手填进不可用列。  
ICS URL 只读 `.obsidian/plugins/obsidian-full-calendar/data.json`，勿写入笔记。
