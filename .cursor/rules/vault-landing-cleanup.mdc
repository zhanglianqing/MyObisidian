---
description: 工作流落地确认后清理过程文件，只保留可复用脚本与定稿 workflow
alwaysApply: true
---

# Vault 落地与清理

## 何时清理

用户明确表示某次**落地/试点「测试满意」「可以定稿」**后，在当次任务收尾时执行清理（无需用户逐文件点名）。

## 保留

- **定稿 workflow**：`0 工作流/` 下编号文档（如 `3.2 Workflow …`）及变更记录
- **可复用脚本**：`0 工作流/scripts/` 中纳入 README 的正式脚本
- **Cursor 规则**：`.cursor/rules/` 中与已落地能力对应的 `.mdc`
- **用户内容**：`Clippings/`、项目笔记等业务产出（非试验草稿）

## 删除或不要新建

- 一次性调试脚本：`_debug_*.py`、临时 PS1、废弃 classify 脚本等
- 重复测试笔记：同主题带 `_HHMMSS` 的 `_Inbox` 草稿（保留最新定稿版）
- 过程说明 md：已被主 workflow 吸收的临时文档
- 已放弃方案的配置残留

## 例外：科研探索

标明 **科研探索 / pilot / 试验** 时：不删尝试记录；在 `5 分支项目/` 等目录保存「假设 → 做法 → 结果」。仅当并入正式 workflow 时才清理该探索下的过程垃圾。

## 收尾

1. 更新主 workflow 变更记录与对照表
2. 确认 README 与 rules 与实现一致
3. 删除过程文件后简短告知用户
