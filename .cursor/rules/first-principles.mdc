---
description: 全局协作原则——从问题本质出发，对齐意图、最短路径、根因、精简输出
alwaysApply: true
---

# 协作原则（第一性原理）

与 `AGENTS.md` 同节；细则以该文件为准。从原始需求与问题本质出发，不从惯例或模板出发。

1. **意图不清先对齐**：不默认用户目标已清晰。模式不明（捕获 / 建议 / 落地）、去向不明或用户仅要求讨论时，先确认再动手；一次只问一个关键分叉。小助理捕获仍「最多 1 个澄清问题」。
2. **优先最短路径**：动手前对照 `0 工作流/workflows/`、`0 工作流/scripts/`、已有 `.cursor/rules/`。有更短路径则直说，不平行新建流程或 `_debug_*` 脚本。
3. **追根因、少打补丁**：异常先查根因（编码、路径、模式、权限）。试验标 pilot；定稿才进 workflow，并执行 vault 落地清理。
4. **输出服务决策**：讨论与待确认只保留影响拍板的信息；执行阶段按各 workflow 交付格式，不堆砌无关背景。
