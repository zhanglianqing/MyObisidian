---
description: 用户说「小助理」时捕获到收件箱；「小助理整理」时合并 drop 并归类
globs:
  - 0 工作流/小助理收件箱.md
  - 0 工作流/_assistant_drop/**/*
alwaysApply: false
---

# 小助理 · 捕获与整理

## 触发

| 触发 | 模式 |
|------|------|
| 消息以 **小助理** 开头，或含 **只记录** / **捕获**（且不含「整理」） | **捕获** |
| 消息含 **小助理整理** 且含 **下周** / **新周** / **本周工作台** / **周规划** / **周度** | **周度整理** → `.cursor/rules/weekly-prep.mdc`（先拉邮件） |
| 消息含 **小助理整理**（无上述周度词） | **整理** |

## 捕获模式

### 必须

1. 解析用户输入中的每一项（换行、分号、编号列表均可拆分）
2. 归类：`待办` / `想法` / `通知` / `杂项`（用户未说明时按语义推断；无法判断时默认 **杂项**，最多问 **1** 个澄清问题）
3. **追加**到 `0 工作流/小助理收件箱.md` 对应 `##` 章节
4. 行格式：`- [ ] YYYY-MM-DD HH:mm | 类型 | 一句话摘要`（摘要保持用户原意，不扩写）
5. 更新 frontmatter `updated` 为当天日期
6. 回复：**仅**列出本次新增条目 + 一句「已记入收件箱，未执行」

### 禁止

- 不运行脚本、不改代码、不用 TodoWrite
- 不展开方案、不给建议、不写步骤
- 不写入 [[同期项目-下一节点]]、[[本周工作台]]、各 `00_ResearchTrack.md`
- 不把条目直接迁到其他项目页

## 整理模式

### 必须

1. 若 `0 工作流/_assistant_drop/` 根目录有待合并文件，先运行：

```powershell
cd "c:\Users\41516\Nutstore\1\MyObisidian\0 工作流\scripts"
.\Merge-AssistantDrop.ps1
```

2. 读取 `0 工作流/小助理收件箱.md` 中所有 `- [ ]` open 项
3. 按用户指令讨论归类，或列出清单供用户选择目标（track、`[[日常随想]]`、[[同期项目-下一节点]] 等）
4. 仅在用户**明确同意**后，才勾选/移出收件箱或写入目标页

### 禁止

- 整理模式下也不要擅自执行科研/代码任务
- 不要未经确认就批量清空收件箱

## 批量输入示例

用户：

```text
小助理：
- 下周问 MDI 排班
- idea：cursor 规则按小助理分流
- 通知：基金附件还没下完
```

写入三行到对应分区，回复三行摘要即可。
