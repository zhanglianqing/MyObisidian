# Codex vault rules

本文件是给 Codex 的 vault 级操作约定，用来承接 `.cursor/rules/` 的工作流精神。Cursor 侧见 `.cursor/rules/first-principles.mdc`；Codex 以本文件为准。

## 协作原则（第一性原理）

从原始需求与问题本质出发，不从惯例或模板出发；与下文各 workflow 口令、默认边界一并生效。

1. **意图不清先对齐**：不默认用户目标已清晰。模式不明（捕获 / 建议 / 落地）、去向不明（收件箱 / 项目页 / 工作台）或用户仅要求讨论时，先确认再动手；一次只问一个关键分叉。小助理捕获仍遵守「最多 1 个澄清问题」。
2. **优先最短路径**：动手前对照 `0 工作流/workflows/`、`0 工作流/scripts/`、已有 `.cursor/rules/`。若已有脚本、Skill 或 workflow 更短，直说并建议走该路径，不平行新建流程或临时脚本。
3. **追根因、少打补丁**：异常先查根因（编码、路径、模式、权限），不对症加规则或 `_debug_*` 脚本。试验标 pilot；定稿才写入 workflow 并执行 `vault-landing-cleanup.mdc` 落地清理。
4. **输出服务决策**：讨论与待确认只保留影响拍板的信息；执行阶段按各 workflow 交付格式（如小助理只列条目、邮件整理用摘要卡片），不堆砌不改变决策的背景。

## 默认边界

- 捕获可以自动落地到 vault 的收件箱/队列。
- 整理可以读队列、列建议、生成摘要卡片。
- 迁移、归档、删除、写入项目页、写入 `10 杂志审稿/`、`11 投稿征召/`、`12 基金申报/`、`同期项目-下一节点.md`、`本周工作台.md` 前，必须得到用户明确的“确认”。
- 用户明确说“只记录不执行”时，只做捕获，不展开方案、不执行任务。
- 不要读取 `0 工作流/_email_drop/` 里的单封邮件，除非用户明确要求邮件整理或点名读取某封。

## 文本编码

- vault 内 Markdown / text 文件默认使用 **UTF-8**；Markdown 不强制 BOM，不要因为没有 BOM 就判断文件有问题。
- CSV / TSV 等面向 Windows Excel 双击打开的文本表格，优先使用 **UTF-8 with BOM**，以避免中文乱码。
- 在 PowerShell 中读取中文文件时，显式使用 `-Encoding UTF8`，并设置输出为 UTF-8，避免控制台误判为 ANSI/GBK 导致乱码。
- 新建或重写 CSV / TSV 时，若预期用 Excel 直接打开，应写入 UTF-8 BOM。
- 不要因为终端显示乱码就判断文件损坏；先用 UTF-8 方式重新读取并按需确认 BOM/字节头。

## 已跑通内容沉淀

- 用户表示某个流程、方法、口令、协作方式“已跑通”“能 work”“以后就这么用”时，Codex 应主动把清理完成的经验落实成 Markdown。
- 可自行判断合适位置：全局规则写入 `AGENTS.md`；工作流方法写入 `0 工作流/workflows/`；工具细节写入 `0 工作流/工具箱/`；特定领域协作习惯写入对应目录 README。
- 沉淀内容应是可复用的简明规则、步骤、口令或模板，不记录冗长聊天过程。
- 若涉及迁移、归档、删除、写入受保护项目页，仍按“默认边界”先取得用户明确确认。

## 小助理

触发：用户消息以“小助理”或“小助手”开头，或含“只记录”“捕获”，且不含“整理”。

动作：
- 读取并更新 `0 工作流/小助理收件箱.md`。
- 将条目归入 `待办` / `想法` / `通知` / `杂项`。
- 自动追加一行：`- [ ] YYYY-MM-DD HH:mm | 类型 | 一句话摘要`。
- 更新 frontmatter `updated` 为当天日期。
- 回复列新增条目，并说明“已记入收件箱，未执行”。
- **工作台同步（须先确认）**：若用户说的是**具体内容**（非模糊碎片），且判断适合同步到 `0 工作流/本周工作台.md`、`0 工作流/同期项目-下一节点.md` 或 `0 工作流/日程/科研推进/本周科研推进.md`，则**列出建议变更并主动请用户确认**；用户确认后再写入。一次可合并为一条确认问题。

禁止：
- 不展开计划、不运行科研或代码任务。
- **不擅自**写入工作台、下一节点、科研推进、各 `00_ResearchTrack.md`、项目页或 `10/` `11/` `12/`。
- 适合同步但未获确认时，只完成收件箱捕获，不代写上述页面。

## 小助理：已完成生活记录

触发：用户说明某条不是待办、而是已经发生/已经完成的生活记录，并要求“有个地方记录”；或内容明显属于生活、健康、活动等非项目 track 的个人记录。

动作：
- 可主动判断合适去处，优先整理到 `6 其他活动/` 下已有分区；没有合适分区时，可按现有编号新建子文件夹（如 `6.2 生活健康记录`）和轻量记录页。
- 若暂时不确定分类或去处，再询问用户；不要要求用户每次都明确指定目标位置。
- 若只适合临时捕获，先在 `0 工作流/小助理收件箱.md` 的 `杂项` 中记录为已完成条目，格式为 `- [x] YYYY-MM-DD HH:mm | 杂项 | 已完成记录：一句话摘要`。
- 若从收件箱迁移到记录页，把收件箱原条目标记为“已迁移至 [[目标页]]”。
- 记录页优先使用轻量日志格式：frontmatter 写 `type: activity-log`、`category`、`updated`，正文按日期分组记录。

边界：
- 这类迁移只适用于生活、健康、活动等非项目 track 的个人记录。
- 不写入 `本周工作台.md`、`同期项目-下一节点.md`、科研推进页或任何项目页。
- 若目标涉及医疗建议、复诊安排、费用报销、项目任务等后续行动，只记录事实，不自动展开计划；需要用户另行明确要求。

## 单日日记

触发：用户要求“记录今天的情况”“写今天日记”“单日日记”，或描述当天已经发生的工作/生活状态，并希望形成比收件箱更完整的记录。

动作：
- 单日日记统一写入 `0 工作流/日程/Daily/YYYY-MM-DD.md`，不要散落在 vault 根目录。
- 若当天文件不存在，可按 `daily-optional` frontmatter 新建；若已存在，在合适小节追加或扩展。
- 内容不要只做一句话流水账；优先整理为“今日概况 / 发生的事 / 推进状态 / 可能需要分流 / 暂不处理”等轻量结构。
- 可在日记中主动列出建议分流方向：`本周工作台.md`、`同期项目-下一节点.md`、`本周科研推进.md` 或对应项目页。

边界：
- 日记本身可以直接写入 `0 工作流/日程/Daily/`。
- 若用户给出**具体**工作安排、优先级、进展或排期，且适合同步到工作台 / 下一节点 / 科研推进，按与小助理相同的规则：**列出建议变更并主动请用户确认**，确认后再写入。
- 若内容偏流水账或尚不足以决策，只写日记并列分流建议，不主动发起工作台确认。
- 写入项目页或 `10/` `11/` `12/` 仍须单独确认。

## 小助理整理

触发：用户说“小助理整理”。

动作：
- 可读取 `0 工作流/小助理收件箱.md` 的 open 项。
- 列出建议去向和需要确认的条目。
- 只有用户明确说“确认”后，才勾选、迁移或写入目标页。

## 邮件

触发：用户消息以“邮件”开头，且不含“整理”。

动作：
- 自动在 `0 工作流/_email_drop/` 新建 drop 文件。
- 保留用户粘贴原文，不解析截止日，不写入项目页。
- 可刷新或提示刷新 `0 工作流/邮件待整理队列.md`。
- 回复“已入队，未解析”。

## 邮件整理

触发：用户说“邮件整理”。

动作：
- 读取 `0 工作流/邮件待整理队列.md` 和必要的 `_email_drop/` 待处理邮件。
- 输出摘要卡片、分类、截止、建议路径。
- 用户确认前，不写入 `10/11/12`，不删除 drop。
- 用户确认后，写入目标页并**删除**对应 `_email_drop/` 文件，刷新 [[0 工作流/邮件待整理队列]]。

## 周工作台

路径锚点：
- `0 工作流/00 工作流导航.md`（总入口）
- `0 工作流/本周工作台.md`
- `0 工作流/同期项目-下一节点.md`
- `0 工作流/日程/科研推进/本周科研推进.md`

周度整理时，先列出建议和冲突；改工作台、下一节点或科研推进前，需要用户确认。
