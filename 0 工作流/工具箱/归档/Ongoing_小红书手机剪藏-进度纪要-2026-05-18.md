# 小红书手机剪藏 — 进度纪要（2026-05-18）

> **用途**：今日会话收尾，明天从此文件接着做。  
> **主文档**：[[0 工作流/workflows/3.2 Workflow ：社交内容剪藏（公众号与小红书）]] §二、§七  
> **脚本说明**：[[0 工作流/scripts/README-clippings]]

---

## 一、今天已跑通（可日常使用）

### 1. 手机 → 家里 PC → 入库

| 组件 | 状态 |
|------|------|
| iOS 快捷指令 **「剪藏到 obsidian」** | ✅ 已通（POST 表单 `text`=剪贴板） |
| `xhs_clip_service.py`（:8765） | ✅ |
| `clip_xhs_auto.py` 抓取 | ✅ |
| **xhslink 短链** | ✅ 已支持跳转 |
| **多图帖** | ✅ 全图下载到 `_assets/<noteId>/`，笔记内 `## 配图（共 N 张）` |

**快捷指令要点：**

- URL：`http://192.168.3.156:8765/clip`（家里同 Wi‑Fi；IP 以 `Show-XhsClipUrls.ps1` 为准）
- 方法：**POST**；请求体：**表单**；字段 **`text`** = **剪贴板**
- Header：`X-Clip-Token` = `0 工作流/scripts/xhs-clip-token.txt` 内值
- 顺序：先 **复制链接** → 再运行快捷指令
- 成功通知：`{"ok":true,"queued":"....json",...}`

**日志：** `0 工作流/scripts/xhs-clip-receiver.log`  
**失败队列：** `Clippings/_Inbox/_xhs_queue/_failed/`

### 2. 入库后自动要点（Gemini 多模态）

| 组件 | 状态 |
|------|------|
| `enrich_xhs_clipping.py` | ✅ 用户已测可用 |
| 配置 | `xhs-llm.json`（从 `xhs-llm.example.json` 复制；**勿提交 Git**） |
| 默认 | `provider: gemini`，读本地配图 + 视频转写写要点 |

抓取成功后会自动 enrich（若存在 `xhs-llm.json`）；否则 `status: inbox`，可手动：

```powershell
cd "e:\Obisidian\MyObisidian\0 工作流\scripts"
python enrich_xhs_clipping.py "Clippings/Xiaohongshu/_Inbox/某笔记.md" --force
```

### 3. 文档与脚本

- **§七 不同网络环境** 已写入 3.2 workflow
- 新增脚本见下文「脚本清单」

---

## 二、进行一半 / 明天优先

### 0. 坚果云入队 + iOS WebDAV 快捷指令（⏸ 暂缓）

| 项 | 状态 |
|----|------|
| PC 端 | ✅ `xhs_clip_service` 已支持 `.txt` 入队、`--queue-only` |
| iOS「存储文件 → 坚果云」 | ❌ 快捷指令里坚果云仅见 `log`，选不到 `_xhs_queue`（Files 扩展限制） |
| 拟定方案 | WebDAV PUT（§7.1 已写步骤）；需应用密码 + 确认 **队列路径** |
| 用户决定 | **暂缓**，暂不继续配置 |

**恢复时：** 3.2 workflow **§7.1**；家里 Wi‑Fi POST 仍可用（`192.168.x.x:8765/clip`）。

### 1. Tailscale 跨网（未做完，与坚果云方案二选一即可）

| 项 | 状态 |
|----|------|
| PC Tailscale | ❓ 未确认是否已装/登录（`winget install Tailscale.Tailscale`） |
| iPhone Tailscale | ❌ **中国区 App Store 无官方 App** |
| 「Web Finder for Tailscale」 | ❌ **不是正版，不要用** |
| 正版 | App Store 搜 **Tailscale**（Tailscale Inc.），常需 **美区/港区/台区 Apple ID** |

**明天二选一：**

- **A. 跨网**：外区 ID 装 iPhone Tailscale → PC/手机同账号 → `Show-XhsClipUrls.ps1` 取 `100.x.x.x` → 快捷指令改 URL  
- **B. 仅家里**：快捷指令继续用 `192.168.3.156`；外出不能剪藏，回家再跑

### 2. 开机自启 + 防火墙（可能未点 UAC）

管理员 PowerShell **执行一次**（若未做过）：

```powershell
cd "e:\Obisidian\MyObisidian\0 工作流\scripts"
.\Setup-XhsClipAutostart.ps1 -InstallTailscale -StartNow
```

- 会弹 UAC，需点「是」
- 注册计划任务：**`XhsClipService`**（登录自启）
- 防火墙入站 **TCP 8765**（Private + Domain）

**检查是否已注册：** 任务计划程序 → `XhsClipService`  
**手动启动服务：** `.\Start-XhsClipService-Background.ps1`  
**看 URL：** `.\Show-XhsClipUrls.ps1`

### 3. PC 常驻

- 插电时尽量 **不休眠**（外出剪藏依赖家里 PC 在线）
- Gemini 走 **PC 代理/VPN**（与手机 POST 无关）

---

## 三、链路备忘（三段网络）

```text
A  手机 POST → 家里 PC :8765     （快捷指令 / Tailscale 或 LAN）
B  PC → 小红书                   （xhs-cookies.json）
C  PC → Gemini API               （xhs-llm.json，国内常要代理）
```

| 现象 | 先查 |
|------|------|
| 快捷指令失败 | A：health、token、URL、本地网络权限 |
| 无 md | B：`_xhs_queue/_failed/`、Cookie |
| 无要点 | C：`xhs-llm.json`、Gemini 网络、`enrich ... --force` |

---

## 四、脚本清单（`0 工作流/scripts/`）

| 文件 | 用途 |
|------|------|
| `xhs_clip_service.py` | 手机 POST 接收 + 队列 |
| `Start-XhsClipService.ps1` | 前台启动（调试） |
| `Start-XhsClipService-Background.ps1` | 后台启动 |
| `Setup-XhsClipAutostart.ps1` | 防火墙 + 登录自启（**需管理员**） |
| `Show-XhsClipUrls.ps1` | 打印 LAN / Tailscale URL |
| `clip_xhs_auto.py` / `fetch_xhs_note.py` | 抓取、多图 |
| `enrich_xhs_clipping.py` | Gemini 自动要点 |
| `xhs-llm.json` | API Key（本地，gitignore） |
| `xhs-clip-token.txt` | 快捷指令 Header（gitignore） |

---

## 五、测试笔记（可参考）

- `Clippings/Xiaohongshu/_Inbox/2026-05-18 各年龄段英语启蒙实操规划＋分阶书单推荐.md`  
  - 18 张配图已本地化；要点已手写/可再 `--force` enrich

---

## 六、明天建议顺序（约 15 分钟）

1. `.\Show-XhsClipUrls.ps1` + Safari 测 `.../health`
2. 若未做：管理员运行 `.\Setup-XhsClipAutostart.ps1 -StartNow`（Tailscale 视情况加 `-InstallTailscale`）
3. 决定跨网方案（Tailscale 或仅 LAN）
4. 复制链接 → 快捷指令 → 看 `_Inbox` 新 md 是否 `classified`
5. （可选）把 §五对照表里「AI 要点」一行改为「Gemini enrich 自动」

---

## 七、安全提醒

- 若曾把 API Key 写进 `xhs-llm.example.json` 并提交过 Git，建议在平台 **轮换密钥**。
- `xhs-clip-token.txt` 勿分享。

---

## 变更记录

| 日期 | 说明 |
|------|------|
| 2026-05-18 | 会话纪要：手机剪藏通、多图、Gemini enrich、Tailscale/自启待完成 |
